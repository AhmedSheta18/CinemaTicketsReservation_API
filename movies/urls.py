from django.urls import path, include

app_name = 'movies'

urlpatterns = [
    # Define your movie-related URLs here
    # Example: path('list/', views.MovieListView.as_view(), name='movie-list'),
    # path('detail/<int:pk>/', views.MovieDetailView.as_view(), name='movie-detail'),
    # path('create/', views.MovieCreateView.as_view(), name='movie-create'),    
    # path('update/<int:pk>/', views.MovieUpdateView.as_view(), name='movie-update'),
    # path('delete/<int:pk>/', views.MovieDeleteView.as_view(), name='movie-delete'),
    # path('reviews/', include('movies.reviews.urls', namespace='reviews')),        
    # path('categories/', include('movies.categories.urls', namespace='categories')),
    # path('actors/', include('movies.actors.urls', namespace='actors')),
    ]
